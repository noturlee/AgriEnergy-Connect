@{
    ViewData["Title"] = "Add a Farmer";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <script src="https://cdn.jsdelivr.net/npm/emailjs-com@2.6.4/dist/email.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            emailjs.init("Vsq3Hdp3PRqXrNsqv"); // Replace with your actual EmailJS user ID
        });

        function sendMail() {
            var params = {
                sendername: "AgriEnergy Admin", // Fixed sender name
                to: document.querySelector("#to").value,
                subject: "Farmer Invitation", // Fixed subject
                reply_to: "admin@agri.co.za", // Fixed reply-to email
                message: "You have been invited to join the AgriEnergy-Connect platform. Please tap the link below to complete your registration:" // Fixed message with link
            };

            var serviceID = "service_9mvl2ik";
            var templateID = "template_dblaltb";

            emailjs.send(serviceID, templateID, params)
                .then(function (response) {
                    alert("Email Sent Successfully");
                }, function (error) {
                    alert("Failed to send email. Please try again later.");
                    console.error('EmailJS error:', error);
                });
        }
    </script>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin-top: 170px;
            background-image: url('https://images.unsplash.com/photo-1510274460854-4b7ad642d3a9?q=80&w=1770&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
            background-position: 300px top; /* Adjust the value to start 300px away from the left */
            background-repeat: no-repeat; /* Ensure the background image doesn't repeat */
            background-size: 1650px 950px; /* Set a specific size for the background image */
            background-attachment: fixed; /* This keeps the image fixed while scrolling */
        }

        h1 {
            text-align: center;
            color: #333;
        }

        form {
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin: 15px 0 5px;
            font-weight: bold;
        }

        input, button {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        button {
            background-color: darkgoldenrod;
            color: white;
            border: none;
            cursor: pointer;
        }

            button:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <h1>Add a Farmer</h1>
    <br />
    <br />
    <form onsubmit="sendMail(); return false;">
        <label for="to">Recipient</label>
        <input type="email" id="to" required />

        <button type="submit">Send Email</button>
    </form>
</body>
</html>
